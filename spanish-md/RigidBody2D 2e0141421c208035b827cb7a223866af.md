# RigidBody2D

**RigidBody2D** es el nodo para **objetos con física realista**: caen con gravedad, rebotan, ruedan y responden a fuerzas/empujones. Perfecto para cajas, barriles, proyectiles explosivos.

## Propiedades Esenciales

```
linear_velocity: Vector2     # Velocidad (automática)
angular_velocity: float      # Rotación automática
gravity_scale: float         # Gravedad (1.0 = normal)
bounce: float                # Rebote (0-1)
mass: float                  # Peso
```

## Modos De Física:

- **Static**: Sin física (igual StaticBody2D)
- **Rigid**: Física completa
- **Character**: Híbrido (raro)

## Codigo Basico:

```python
extends RigidBody2D

func _ready():
    # Configuración inicial
    gravity_scale = 1.0
    bounce = 0.3
    mass = 2.0

func _on_mouse_entered():
    apply_impulse(Vector2.RIGHT * 500)  # Empujón al hacer hover

# Señal automática: body_entered
func _on_body_entered(body):
    if body.name == "Jugador":
        queue_free()  # Destruye al tocar jugador

```

## Fuerzas VS Velocidad:

```
# 💥 FUERZA (acelera gradualmente)
apply_force(Vector2.RIGHT * 1000)

# ⚡ EMPUJÓN (instantáneo)
apply_impulse(Vector2.UP * 300)

# 🔧 VELOCIDAD directa (cuidado)
linear_velocity = Vector2(200, 0)

```

## Casos de Uso Perfectos

✅ Cajas empujables
✅ Barriles explosivos
✅ Proyectiles físicos
✅ Escombros
✅ Pelotas rebotadoras
✅ Puertas que se abren con física

**Cuándo usar**: **TODO lo que debe reaccionar físicamente** (caer, rodar, rebotar). **NUNCA** controles **`position`** manualmente.